# rock-paper-or-scissors
This Rock, Paper, Scissors game allows the user to play against the computer, which makes a random choice each round. The program determines the winner, displays results, optionally tracks scores, and lets the user play multiple rounds with clear instructions.
